model {
	for (i in 1:N){
		first.d[i] ~ dnorm(pred[i], tau)
		pred[i] <- ma*treatment[i] + mb*line[i] + mab*treatment[i]*line[i] + int
	}
	ma ~ dnorm(0, .0001)
	mb ~ dnorm(0, .0001)
	mab ~ dnorm(0, .0001)
	int ~ dnorm(0, .0001)
	tau ~ dgamma(.001, .001)
}
