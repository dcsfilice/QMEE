model {
	for (i in 1:N){
		first.d[i] ~ dnorm(pred[i], tau)
		pred[i] <- ma*treatment[i] + mb*line[i] + mab*treatment[i]*line[i] + int
	}
	ma ~ dnorm(0, .000001)
	mb ~ dnorm(0, .000001)
	mab ~ dnorm(0, .000001)
	int ~ dnorm(0, .000001)
	tau ~ dgamma(.001, .001)
}
